# dependencies
node_modules/

# env / secrets
.env
.env.*
!.env.example
secrets/
*.pem
*.key

# build / runtime
dist/
.tmp/
.DS_Store

# local artifacts generated during demos
artifacts/

# raw recording / intermediate media; publish only selected final assets intentionally
*.mov
*raw*.mp4
*raw*.mp3
*sample*.mp3
telegram-*.mp4
delegated-mindgames-demo-draft-v[0-7]*.mp4

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local state
RUNSTATE.md

# local bot runtime state
.runtime/
